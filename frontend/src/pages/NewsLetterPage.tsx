import { useEffect, useState } from 'react'

const NewsLetterPage = () => {
  const [inputNewsLetters, setInputNewsLetters] = useState<string[]>()
  return <div>가정통신문</div>
}

export default NewsLetterPage
